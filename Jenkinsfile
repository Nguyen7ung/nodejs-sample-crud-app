pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Build Docker image
                sh 'docker-compose up -d'
            }
        }
        stage('Test') {
            steps {
            }
        }
        stage('Deliver') { 
            steps {
               
            }
        }
    }
}
